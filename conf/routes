# The '@' in front of the action avoids the controller to be used as a singleton.
# see https://www.playframework.com/documentation/2.6.x/JavaDependencyInjection
GET    /test    @webservice.TestController.test
